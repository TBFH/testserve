from setuptools import setup

setup(
    name="testserve",
    version="0.0.2",
    author="austin",
    description="High performance inference engine for LLMs.",
    packages=["testserve"],
    zip_safe=False,
)
