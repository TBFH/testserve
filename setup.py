from setuptools import setup

setup(
    name="testserve",
    version="0.0.1",
    author="austin",
    description="High performance inference engine for LLMs.",
    packages=["testserve"],
    zip_safe=False,
)
